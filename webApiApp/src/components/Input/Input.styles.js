import { StyleSheet } from 'react-native';

export default StyleSheet.create({
    container: {
        margin: 10,
        padding: 5,
        backgroundColor: '#efefef',
        borderRadius: 5,
        flexDirection: 'row',
        alignItems: 'center'
    },
    input: {
      flex: 1,
      fontWeight: 'bold',
      fontSize: 16
    },
})